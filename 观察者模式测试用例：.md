# 观察者模式测试用例：

```C++
	user1 u1;
    user2 u2;
    New n1;
    n1.GetSomeChange("T0");

    n1.attach(&u1);
    n1.attach(&u2);
    n1.GetSomeChange("T1");
    n1.ObserverCount();

    n1.Detach(&u2);
    n1.GetSomeChange("T2");
    n1.ObserverCount();
```

